# Workspace Module Template Notes

This template family is reserved for workspace-scoped modules such as `workspace/task`
and `workspace/memo`.

Keep the generated surface limited to repeatable module skeleton code. Sync semantics,
conflict resolution, and product-specific interaction logic must stay manual.
