{% set entity_snake = entity.name | snake_case %}
{% set entity_pascal = entity.name | pascal_case %}
use uuid::Uuid;
{% for field in entity.fields %}
{% if  "Email" in field.rust_type  %}
use shared_domain::value_objects::Email;
{% break %}
{% endif %}
{% endfor %}

/// Update {{ entity.description | default(value=entity.name) }} Command
#[derive(Debug, Clone)]
pub struct Update{{ entity_pascal }}Command {
    /// {{ entity.name }} ID
    pub {{ entity_snake }}_id: Uuid,
{% if entity.schema == "sys" or  "tenant" in entity.table_name  %}
    /// Tenant ID
    pub tenant_id: String,
{% endif %}
{% for field in entity.fields %}
{% if not field.primary_key and not field.name in entity.hidden_fields %}
{% if field.name == "tenant_id" or field.name == "created_at" or field.name == "updated_at" or field.name == "created_by" or field.name == "updated_by" or field.name == "id" %}
{% continue %}
{% endif %}
    /// {{ field.description | default(value=field.name) }} (optional)
    pub {{ field.name | snake_case }}: Option<{{ field.rust_type }}>,
{% endif %}
{% endfor %}
}

impl Update{{ entity_pascal }}Command {
    pub fn new(
        {{ entity_snake }}_id: Uuid,
{% if entity.schema == "sys" or  "tenant" in entity.table_name  %}
        tenant_id: String,
{% endif %}
    ) -> Self {
        Self {
            {{ entity_snake }}_id,
{% if entity.schema == "sys" or  "tenant" in entity.table_name  %}
            tenant_id,
{% endif %}
{% for field in entity.fields %}
{% if not field.primary_key and not field.name in entity.hidden_fields %}
{% if field.name == "tenant_id" or field.name == "created_at" or field.name == "updated_at" or field.name == "created_by" or field.name == "updated_by" or field.name == "id" %}
{% continue %}
{% endif %}
            {{ field.name | snake_case }}: None,
{% endif %}
{% endfor %}
        }
    }

{% for field in entity.fields %}
{% if not field.primary_key and not field.name in entity.hidden_fields %}
{% if field.name == "tenant_id" or field.name == "created_at" or field.name == "updated_at" or field.name == "created_by" or field.name == "updated_by" or field.name == "id" %}
{% continue %}
{% endif %}
    /// Set {{ field.description | default(value=field.name) }}
    pub fn with_{{ field.name | snake_case }}(mut self, {{ field.name | snake_case }}: {{ field.rust_type }}) -> Self {
        self.{{ field.name | snake_case }} = Some({{ field.name | snake_case }});
        self
    }

{% endif %}
{% endfor %}
}

// <<< block:custom_update_methods
// 自定义更新命令方法（在此添加自定义的更新逻辑）
// >>> end:custom_update_methods
